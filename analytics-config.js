"use strict";

// Add public measurement IDs here after creating the accounts. Empty values keep
// every external analytics request disabled while preserving the event contract.
window.FoxnaimAnalyticsConfig = Object.freeze({
  ga4MeasurementId: "",
  clarityProjectId: "",
  cloudflareToken: ""
});
